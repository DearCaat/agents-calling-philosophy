# agents-calling-philosophy

跨 harness 的 **调用哲学** 插件。本机 inventory / 凭据挂在共享目录 `$AGENTS_DATA_ROOT`（见 README），不进本仓库。

派工时先读：`skills/executing-model-combinations/SKILL.md`。  
数据根解析：`skills/executing-model-combinations/scripts/lib/data-root.sh`。
